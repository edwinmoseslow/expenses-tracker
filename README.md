# expenses-tracker

> A Vue.js Expenses Tracker Project

## Build Setup

``` bash
# install dependencies
npm install

# run client side
cd client

# serve with hot reload at localhost:8080
npm run serve
```